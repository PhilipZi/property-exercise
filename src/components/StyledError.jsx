import styled from "styled-components";

const StyledError = styled.div`
  z-index: 10;
  position: fixed;
  background-color: rgba(148, 38, 38, 0.1);
  height: 100vh;
  width: 100vw;
`;

export default StyledError;
